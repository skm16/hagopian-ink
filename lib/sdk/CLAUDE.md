# wp-headless-kit — Generated SDK Reference

> **AUTOGENERATED.** Do not edit by hand. Regenerate with `jab sync <project-dir>`.
>
> Source: https://hagopianink.wpengine.com · Manifest v1 · Fetched 2026-05-13T14:05:02.292Z · 11 abilities

## What this is

This project consumes a WordPress install exposing the official MCP Adapter (`wordpress/mcp-adapter`) through a typed SDK auto-generated from the live ability schemas. You don't call WP REST endpoints directly — you call typed functions like `getBeers(client)` and get back fully-typed data, including ACF fields when the WP-side post type has any.

## How to use the SDK

```ts
// Server Component — credentials never reach the browser.
import { getBeers } from "@/lib/sdk";
import { jabClient } from "@/lib/jab/client";

export const revalidate = 60; // ISR

export default async function BeersPage() {
  const { beers } = await getBeers(jabClient);
  //      ^^^^^ fully typed, including `acf` when the post type has fields
  return /* render beers */;
}
```

The client (`jabClient`) is server-only — it carries WP credentials and is gated by `import "server-only"`. Importing it from a Client Component fails at build time. For client-side data needs, build a route handler that calls the SDK server-side and returns the data through `fetch`.

## Available abilities

| Function | Input | Returns | ACF |
| --- | --- | --- | --- |
| `getPosts(client, input?)` | `GetPostsInput?` | `GetPostsOutput` | yes |
| `getPostBySlug(client, input)` | `GetPostBySlugInput` | `GetPostBySlugOutput` | no |
| `getPages(client, input?)` | `GetPagesInput?` | `GetPagesOutput` | yes |
| `getPageBySlug(client, input)` | `GetPageBySlugInput` | `GetPageBySlugOutput` | no |
| `getOurWork(client, input?)` | `GetOurWorkInput?` | `GetOurWorkOutput` | yes |
| `getWorksBySlug(client, input)` | `GetWorksBySlugInput` | `GetWorksBySlugOutput` | no |
| `getCategories(client, input?)` | `GetCategoriesInput?` | `GetCategoriesOutput` | no |
| `getTags(client, input?)` | `GetTagsInput?` | `GetTagsOutput` | no |
| `getExpertiseTags(client, input?)` | `GetExpertiseTagsInput?` | `GetExpertiseTagsOutput` | no |
| `getWorkType(client, input?)` | `GetWorkTypeInput?` | `GetWorkTypeOutput` | no |
| `getMenus(client)` | — | `GetMenusOutput` | no |

All input types are optional — every input field declared in the WP-side schemas has a `default`, so callers can pass `{}` (or, for parameterless abilities, omit the argument entirely).

The output types' wrapper key matches the ability's purpose (`{ posts: [...] }`, `{ beers: [...] }`, `{ menus: [...] }`). Per-item shape is `{ id, title, excerpt, date, slug, link, acf }` for CPT-list abilities; menu items have a different shape (id, title, url, target, object_type, object_id, parent_id, order). See `./types.ts` for the full property-by-property schemas.

## ACF fields per ability

### `getPosts` (jab/get-posts)

ACF fields populated when set on a post:
- `post_builder`: array<{ acf_fc_layout, wysiwyg, padding } | { acf_fc_layout, column_left_content, column_right_content, padding } | { acf_fc_layout, column_left_content, column_center_content, column_right_content, … } | { acf_fc_layout, pull_quote_left, column_right_content, padding } | { acf_fc_layout, pull_quote, quote_author, padding } | { acf_fc_layout, image_left, image_right, caption, … } | { acf_fc_layout, image, caption, padding }> — Post Builder
- `journal_post_banner`: { ID, id, url, alt, … } — Featured Image One — Optional banner image must be in 16:9 aspect ratio.
- `featured_image_two`: { ID, id, url, alt, … } — Featured Image Two
- `journal_post_subtitle`: string — Subtitle — Optional subtitle text. Ideally 110 characters or less (can fit in a tweet)
- `journal_short_title`: string — Short Title — Short descriptive title for use in previews, navigation, etc. Ideal ~40 chrs, Max 70 chrs.
- `journal_short_desc`: string — Short Description — Short description text for use in previews, navigation etc. Max 80 characters.

### `getPages` (jab/get-pages)

ACF fields populated when set on a post:
- `header_title`: string — Title
- `title_mobile`: string — Title Mobile
- `header_subtitle`: string — Subtitle
- `header_link`: { title, url, target } — Link
- `header_background_image`: string (uri) — Background image
- `video_webm`: string (uri) — Video ( webm )
- `video_mp4`: string (uri) — Video ( mp4 )
- `template_part`: array<{ acf_fc_layout, title, subtitle, background_color, … } | { acf_fc_layout } | { acf_fc_layout } | { acf_fc_layout, title, subtitle, form, … } | { acf_fc_layout, title, link } | { acf_fc_layout, title, description, link, … } | { acf_fc_layout, background-color, block } | { acf_fc_layout, title, client, slider } | { acf_fc_layout, case, link } | { acf_fc_layout, title, core_value } | { acf_fc_layout, title, persons } | { acf_fc_layout, awards, publications, articles_&_features } | { acf_fc_layout, title, block, link } | { acf_fc_layout, images } | { acf_fc_layout, background_color, name_group, title, … } | { acf_fc_layout }> — Blocks

### `getOurWork` (jab/get-our-work)

ACF fields populated when set on a post:
- `template_part`: array<{ acf_fc_layout, title, links } | { acf_fc_layout, title, description } | { acf_fc_layout, computer, tablet, mobile } | { acf_fc_layout, column } | { acf_fc_layout, image } | { acf_fc_layout, name, title, description, … } | { acf_fc_layout, images } | { acf_fc_layout, nav_bar, full_width, first_page, … } | { acf_fc_layout, title, subtitle, background_color, … } | { acf_fc_layout, title, subtitle, form, … } | { acf_fc_layout, images } | { acf_fc_layout } | { acf_fc_layout, name_group, title, description, … } | { acf_fc_layout, images } | { acf_fc_layout, images } | { acf_fc_layout } | { acf_fc_layout }> — Blocks

## Patterns

### ISR (Incremental Static Regeneration)
Each Server Component opts into per-route revalidation: `export const revalidate = 60`. Default is no revalidation. Tune per-route based on how stale the data can be.

### Error handling
The client throws `JabClientError` (exported from `./client`) on transport, protocol, or ability failures. In Server Components, let them bubble — Next.js's error boundary catches them. In route handlers, catch and return a typed error response.

### Where to put new files
- `lib/sdk/` is **regenerated**. Never edit files here by hand — `jab sync` will overwrite them.
- `lib/jab/` is **hand-crafted glue** (the client wrapper, request helpers, opinionated patterns). Survives regenerations.

### Regenerating after WP-side schema changes
```bash
jab sync <project-dir>
```
Refreshes `.jab/manifest.json` from the saved WP URL and rewrites all four SDK files. Run after any change to ability registration, ACF field groups, or post type registration on the WP side.

### Server-only imports
`lib/jab/client.ts` is gated by `import "server-only"`. Always call SDK functions from Server Components, route handlers, or server actions — never from Client Components. If you need data on the client, fetch it through a route handler that wraps the SDK call.

### Strangler-fig migration (incremental headless)
`app/[[...slug]]/route.ts` is a catch-all handler that proxies any unmatched route to `process.env.WP_PROXY_URL`. Workflow:

1. Set `WP_PROXY_URL` in `.env.local` to the original WordPress site URL.
2. Run `pnpm dev`. Every route falls through to the original site — visually identical to before.
3. Build a Next.js page for one route at a time (e.g. `app/posts/page.tsx`). Explicit local routes always win, so anything you implement immediately replaces the proxied version. Anything you haven't implemented stays proxied.
4. Repeat. Decommission the original site when no routes are still proxying through.

This is the agency-friendly migration story: clients keep their current site working at every commit; you replace pieces on your own schedule.

To opt out, leave `WP_PROXY_URL` empty (the handler returns 404), or delete `app/[[...slug]]/route.ts` entirely.

When pointing at a local-dev host (`*.local`, `*.test`) with a self-signed cert, also set `NODE_TLS_REJECT_UNAUTHORIZED=0` in `.env.local` so Node's fetch accepts the cert. Dev-only — never in production.

## Out of scope (deliberately)

This SDK does not currently emit:
- Form submission, search, preview-mode wiring, multilingual support, WooCommerce. These were explicitly deferred per the project's `CLAUDE.md`.
